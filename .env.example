# Gmail SMTP 群发配置（复制本文件为 .env 并填写真实值；.env 已被 .gitignore 忽略，不会提交）
GMAIL_USER=yourname@gmail.com
GMAIL_PASS=十六位应用专用密码
FROM_NAME=你的品牌名或姓名（可选，留空则用邮箱地址发件）
DELAY_MS=3000
CSV_FILE=leads.csv

# Resend 云端发信（无需写进本地 .env，只需在 Cloudflare Pages 环境变量里设置 RESEND_KEY）
# RESEND_KEY=re_xxxxxxxxxxxxxxxx

